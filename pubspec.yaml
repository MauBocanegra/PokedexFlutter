name: pokedex_pl
description: Pokedex app showcasing Clean Architecture + Cubit + GoRouter
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^17.0.0
  either_dart: ^1.0.0
  get_it: ^9.2.1
  url_launcher: ^6.3.0
  http: ^1.2.2

  flutter_bloc: ^9.1.1

  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true